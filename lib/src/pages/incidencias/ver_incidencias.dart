import 'package:flutter/material.dart';
 

 
class VerIncidencias extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(
          title: Text('Ver Incidencias'),
        ),
        body: Center(
          child: Container(
            child: Text('Hello World'),
          ),
        ));}
}